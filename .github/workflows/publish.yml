name: publish

on:
  release:
    types:
      - published
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag to publish (e.g., v0.5.0)'
        required: true
        type: string

permissions:
  contents: read

jobs:
  publish-npm:
    if: startsWith(github.event.release.tag_name, 'v') || startsWith(inputs.tag, 'v')
    runs-on: ubuntu-latest
    environment: npm-publish
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: ${{ github.event.release.tag_name || inputs.tag }}

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "22"
          registry-url: https://registry.npmjs.org
          package-manager-cache: false

      - name: Upgrade npm for trusted publishing
        run: npm install --global npm@^11.5.1

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install copier
        run: python -m pip install --upgrade pip copier

      - name: Validate release tag + package version
        env:
          TAG_NAME: ${{ github.event.release.tag_name || inputs.tag }}
        run: |
          if [[ ! "$TAG_NAME" =~ ^v[0-9]+\.[0-9]+\.[0-9]+([-.][0-9A-Za-z.]+)?$ ]]; then
            echo "Release tag must match vX.Y.Z (optional prerelease/build suffix)." >&2
            exit 1
          fi

          PKG_VERSION="$(node -p "JSON.parse(require('node:fs').readFileSync('package.json', 'utf8')).version")"
          if [[ "v${PKG_VERSION}" != "$TAG_NAME" ]]; then
            echo "package.json version (${PKG_VERSION}) does not match release tag (${TAG_NAME})." >&2
            exit 1
          fi

      - name: Install dependencies
        run: |
          if [[ -f package-lock.json ]]; then
            npm ci
          else
            npm install --package-lock-only --ignore-scripts
            npm ci
          fi

      - name: Validate template guardrails
        run: npm run check:full

      - name: Release checks (full)
        run: npm run release:check

      - name: Publish to npm (OIDC + provenance)
        run: npm publish --provenance --access public
